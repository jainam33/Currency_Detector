name: currency_detatctor
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter



  cupertino_icons: ^1.0.2
  animated_splash_screen:
  tflite:
  camera:
  path_provider:
  path:
  flutter_tts: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter




flutter:
  assets:
    - assets/labels.txt
    - assets/model_unquant.tflite
    - assets/CD.jpg


  uses-material-design: true

